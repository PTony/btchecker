<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ path('homepage') }}">
        <img alt="btchecker" width="24" src="{{ asset('favicon.png') }}">
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    <div class="collapse navbar-collapse navbar-right" >
      <ul class="nav navbar-nav">
        <li class="active"><a href="{{ path('image_two_random') }}">Voter<span class="sr-only"> (current)</span></a></li>
        <li><a href="{{ path('image_index_mine')}}">Voir mes images</a></li>
        <li><a href="{{ path('image_index')}}">Voir toutes les images</a></li>
        <li><a href="{{ path('image_new')}}">Ajouter une image</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="{{path('fos_user_profile_show')}}">Profile</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="{{ path('fos_user_security_logout') }}">{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}</a></li>
          </ul>
        </li>
      </ul>      
    </div><!-- /.navbar-collapse navbar-right -->
      
    {% else %}
    <div class="collapse navbar-collapse navbar-right">
      <a class="btn btn-default btn-primary navbar-btn" href="{{ path('fos_user_registration_register') }}" role="button">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
      <a class="btn btn-default navbar-btn" href="{{ path('fos_user_security_login') }}" role="button">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
    </div><!-- /.navbar-collapse navbar-right -->

    {% endif %}      
  </div><!-- /.container-fluid -->
</nav>